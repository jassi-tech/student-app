# Example .env (do NOT commit real .env to source control)
# Copy this to .env and fill in values for local development.

# Backend URL used by the app (optional). Leave empty to use internal mock.
BACKEND_URL=

# Mock auth (set to true to bypass backend during local development)
MOCK_ALLOWED=false
MOCK_STUDENT_ID=
MOCK_PASSWORD=
